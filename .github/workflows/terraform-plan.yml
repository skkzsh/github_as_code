name: Terraform Plan

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  terraform-plan:
    runs-on: [ ubuntu-latest ]

    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/terraform
        with:
          should_apply: 'no'
